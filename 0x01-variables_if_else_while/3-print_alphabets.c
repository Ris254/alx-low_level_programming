
#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - A program that prints the alphabet in lowercase,then in uppercase
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	char k;

	for (k = 'a'; k <= 'z'; k++)
		printf(k);
	for (k = 'A'; k <= 'Z'; K++)
		printf(k);
	printf('\n')
	return (0)
}
